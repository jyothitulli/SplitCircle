# Base URL the frontend uses to reach the backend API.
# In dev, Vite proxies /api to this target (see vite.config.js),
# so this is mainly useful for builds/deployments (Phase 21).
VITE_API_URL=http://localhost:4000
